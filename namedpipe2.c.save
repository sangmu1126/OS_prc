#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <stdio.h>
#include <unistd.h>

int main() {
	FILE =*fp;
	char buff[255];
	if ((fp=fopen("testfife", "rw")) == NULL) {
		perror("open error");
		exit(0);
	}
	memset(buf, x00,255);
	while(1){
	while(fgets(buf, 255, hp) != NULL) ( 
		printf("%s\n",buf);
		memset(buf, 0x00,255);
		}
	}
	return 0;
}
